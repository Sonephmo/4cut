# 해솔네컷 키오스크 PRD (v1)

## 1) 제품 개요
- 목적: 인생네컷 부스를 무인 운영 가능한 Windows 키오스크 앱으로 구현한다.
- 핵심 가치: 인쇄 성공률 최우선, 현장 운영 복구 가능성 확보, 사용자 흐름 단순화.
- 대상 환경: Windows 노트북 + Canon SELPHY CP1500.

## 2) 이번 합의사항 (문답 확정)
- 범위: `A` (로컬 완결 우선)
  - 포함: 촬영/선택/합성/인쇄/기본 운영자 기능.
  - 제외: Supabase 업로드, Viewer QR 전달, Edge Function.
- Supabase 모드: `A` (운영 단순 모드)  
  - 단, 범위 A에 따라 2단계에서 적용.
- UI 구현 기준: `B`
  - `UXUI.md` 기능 규칙을 지키면서 `design/*.md` 레이아웃/톤을 최대 반영.

## 3) v1 MVP 범위 (로컬 완결)

### 3.1 사용자 플로우
- `1_MAIN` 프레임 선택
- `3_quantity` 인쇄 장수 선택
- `4_camera` 촬영 안내
- `4_3 Shooting` 10장 촬영
- `6_select` 4장 선택 (선택 순서 = 배치 순서)
- `5_loading` 합성/인쇄 상태 표시
- `7_printing` 인쇄 전용 강조 화면 (분리 유지)
- `8_end` 완료 화면

### 3.2 기능 요구사항
- 촬영 10회, 각 촬영 즉시 로컬 저장.
- 촬영 타이밍: 카운트다운 10초, 촬영 간 간격 2초.
- 10장 중 4장 선택, 선택 순서 배지(1~4) 표시.
- 4x6(1800x1200) 2x2 템플릿 합성(sharp), 프레임 오버레이 지원.
- silent print 수행, 복수 인쇄(copies) 지원.
- 인쇄 실패 시 사용자 안내 + 운영자 개입 재시도.
- 로컬 세션/로그 저장(SQLite + 파일 로그).

### 3.3 운영자 기능 (MVP 최소)
- 핫키 + PIN 진입.
- PIN은 `.env` 주입값 사용(코드 하드코딩 금지).
- 프린터 선택/테스트 인쇄.
- 카메라 장치 확인.
- 강제 홈 복귀, 앱 재시작.

## 4) 비범위 (MVP 제외)
- Supabase 업로드/Viewer URL/QR 표시.
- FAILED_UPLOAD 재시도 큐.
- Edge Function 기반 보안 업로드 경로.

## 5) 2단계 확장 범위 (v1.5)
- Supabase 연동(운영 단순 모드: service role 사용).
- 최종 이미지 업로드 + viewer URL 생성 + QR 완료 화면.
- 업로드 실패 재시도 큐 및 운영자 재처리.

## 6) 성공 기준 (MVP)
- 정상 플로우에서 `10장 촬영 → 4장 선택 → 합성 → 인쇄` 1회 완주.
- 프린터 연결 정상 시 인쇄 성공률 95% 이상(현장 테스트 기준).
- 인쇄 실패 시 앱 크래시 없이 운영자 재시도 가능.
- 강제 종료 후 재실행 시 앱이 기본 화면으로 안전 복귀.

## 7) 기술 스택
- Electron + Vite + React + TypeScript
- 이미지 합성: sharp
- 로컬 DB: better-sqlite3
- 로깅: pino(or winston)
- 패키징: electron-builder

## 8) 구현 우선순위
1. 프로젝트 부트스트랩(Electron/Renderer/Preload/IPC 뼈대)
2. 촬영 저장 파이프라인(shots:save, shots:list)
3. 선택 화면 + 4장 규칙
4. 템플릿 합성 + 프레임 오버레이
5. PrintWindow + silent print + 실패 처리
6. 운영자 패널 최소 기능
7. 패키징/자동실행/키오스크 설정
8. (2단계) Supabase/QR 확장

## 9) 오픈 이슈
- 기본 프린터 장애 처리 UX 문구 최종 확정 필요.
- 실제 사용 프레임 자산(PNG 원본) 정리 필요.
